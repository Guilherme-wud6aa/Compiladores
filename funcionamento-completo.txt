program exemplo ;
var
    x, y : integer ;
    nome : string ;
begin
    read(x, y);
    if (x > 0) then {
        write("x eh positivo");
        read(z);
   } else {
	  write("x eh negativo");
   }
    while (x > 0) do {
        x := x - 1;
   }
    write(x, y);
end.
